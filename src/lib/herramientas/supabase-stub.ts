// @ts-nocheck
/**
 * Stub de cliente Supabase para el subárbol migrado de clarity-dashboard.
 * Los caches (api-cache.server / history-cache.server) degradan
 * automáticamente a caché en memoria cuando el cliente es null,
 * así que en este proyecto la capa L2 (Supabase) queda deshabilitada.
 */
export const supabase: null = null;
