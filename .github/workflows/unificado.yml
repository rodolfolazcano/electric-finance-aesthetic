name: unificado — CEDEARs 11-23 ART direct Telegram (bypass Vercel)

on:
  schedule:
    # 11-23 ART = 14-02 UTC. GH cron es UTC.
    # 14,15,16,17,18,19,20,21,22 UTC (11-19 ART) + 0,1,2 UTC (21-23 ART del día anterior)
    - cron: "0 14,16,18,20,22 * * 1-5"
    - cron: "30 14,16,18,20,22 * * 1-5"
    - cron: "0 0,1,2 * * 2-6"
  workflow_dispatch:
    inputs:
      force:
        description: "Forzar 1 demo aunque scanner de 0"
        required: false
        default: "false"

jobs:
  unificado:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci --ignore-scripts
        working-directory: electric-finance-aesthetic-main
      - run: npx tsx scripts/unificado-prueba.ts ${{ inputs.force == 'true' && '--force' || '' }}
        working-directory: electric-finance-aesthetic-main
        env:
          TELEGRAM_BOT_TOKEN: "8984569191:AAE_p-0OdWv4REoicaKEBeAA3UO1wP9k1So"
          TELEGRAM_CHAT_ID: "8179198652"
          TELEGRAM_CHAT_IDS: "8179198652"
          TELEGRAM_ENABLED: "true"
          TELEGRAM_AGENT_BOT_TOKEN: "8947154888:AAHtQG4zeBw42rTcASv1jyTQn9YByl0HIr0"
          TELEGRAM_WEBHOOK_SECRET: "coronar_whsec_fpxbs777_9c41e7a2b8d3"
          CRON_SECRET: "coronar_cron_9c41e7a2b8d3f1x6k7p0q2w4e8r1t5y9u"
